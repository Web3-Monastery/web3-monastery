{ config, ... }:

{
  security.acme = {
    defaults.email = "";
    acceptTerms = true;
    defaults = {
      dnsProvider = "cloudflare";
      credentialsFile = config.secrix.system.secrets.cloudflare-certs.decrypted.path;
    };
  };
}